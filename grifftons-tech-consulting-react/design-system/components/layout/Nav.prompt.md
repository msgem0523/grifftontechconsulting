```jsx
<Nav items={[{id:'work',label:'Work'},{id:'about',label:'About'},{id:'writing',label:'Writing'}]}
     active="work" onNavigate={go} action={<Button size="s">Get in touch</Button>} />
```
The active link carries a 2px saffron underline — the same marker `<a>` uses in prose.
