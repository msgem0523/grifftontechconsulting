The generic surface — reach for `ProjectCard` for work, this for everything else.

```jsx
<Card tone="outline" pad={28}><Text type="display" sizing="XS">Services</Text></Card>
<Card interactive>…</Card>
```
Interactive cards lift 3px and deepen their shadow. Never combine a border with a shadow.
