```jsx
<Avatar size={120} src={photo} />
<Avatar size={40} initials="TG" tone="saffron" />
```
The initials fallback is the closest thing this brand has to a mark — same face, same tracking as the wordmark.
